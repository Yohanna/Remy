'use strict';
const db = require('../../db/db');


function getRecentSearch(req, res) {
  res.sendStatus(200);
}


function addRecentSearch(req, res) {
  res.sendStatus(200);
}

module.exports = {
  getRecentSearch,
  addRecentSearch
};
